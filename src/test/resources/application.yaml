spring:
  application:
    name: FlutterToDo
  datasource:
    dbname: testdb
    username: testuser
    password: testpassword
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
    drop-first: true
    change-log: db/changelog/changelog-master.yaml
    user: testuser
    password: testpassword
    driver-class-name: org.postgresql.Driver
    liquibase-schema: public
app:
  jwtSecret: testSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecrettestSecret
  jwtExpirationMs: 60000
  jwtRefreshExpirationMs: 3600000
  redis:
    host: localhost
    port: 6379
